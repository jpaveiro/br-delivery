import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    contentWrapper: {
        display: 'flex',
        flexDirection: "row",
        justifyContent: 'space-around'
    },

    image: { 
        width: 100,
        height: 100,
    },

    label: {
        textAlign: 'center'
    },

    restaurantContainer: {
    }
});
